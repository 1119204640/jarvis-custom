# Jarvis 的 LLM 凭据通过设置页面录入，运行时保存在 data/credentials.yaml。
# 只有 Google Calendar OAuth 仍然需要在 .env 中配置。
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8000/api/events/google/callback
